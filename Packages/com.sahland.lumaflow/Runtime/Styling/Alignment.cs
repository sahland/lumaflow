namespace LumaFlow {

    /// <summary>
    /// Specifies one of the supported positions for a child within its parent.
    /// </summary>
    public enum Alignment {
        TopLeft,
        TopCenter,
        TopRight,
        CenterLeft,
        Center,
        CenterRight,
        BottomLeft,
        BottomCenter,
        BottomRight
    }

}
